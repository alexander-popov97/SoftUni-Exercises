function employees(arr) {
	for (let i = 0; i < arr.length; i++) {
		console.log(`Name: ${arr[i]} -- Personal Number: ${arr[i].length}`)
	}
}
employees([
	'Silas Butler',

	'Adnaan Buckley',

	'Juan Peterson',

	'Brendan Villarreal',
])
